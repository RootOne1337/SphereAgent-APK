# SphereAgent v1.8.1 - Критическое обновление

**Дата релиза:** 2 января 2026  
**Тип:** CRITICAL FIX - Обязательное обновление  
**Размер APK:** 23 MB  
**versionCode:** 14

---

## 🚨 КРИТИЧЕСКОЕ ИСПРАВЛЕНИЕ

### Проблема
Input команды (tap/swipe/keyevent) не работали на эмуляторах с root доступом.

**Причина:**
- Приоритет выполнения: Accessibility Service → Shell команды
- Shell команды без root блокируются Android на эмуляторах
- Root доступ был доступен, но не использовался правильно

### Решение
**Новый приоритет выполнения: Root → Accessibility → Shell**

```kotlin
// Теперь команды пытаются использовать root ПЕРВЫМИ:
1. checkRoot() - проверка наличия su
2. executeRootCommand() - выполнение через su -c
3. Accessibility Service - если root недоступен
4. Shell fallback - последняя попытка
```

---

## ✅ Что исправлено

### Root команды
- ✅ Добавлен метод `executeRootCommand()` для гарантированного выполнения через su
- ✅ Изменён приоритет в `tap()`, `swipe()`, `longPress()`, `keyEvent()`
- ✅ Добавлено детальное логирование для диагностики
- ✅ Input команды теперь работают на всех эмуляторах с root

### Диагностика
```kotlin
Log.d(TAG, "tap($x, $y) - hasRoot=${hasRoot}, accessibility=${isServiceEnabled()}")
Log.d(TAG, "ROOT command: $command")
Log.d(TAG, "ROOT command SUCCESS")
```

---

## 📦 Автообновление

### FORCE UPDATE включен ⚡
```json
{
  "version": "1.8.1",
  "force_update": true
}
```

**Что это значит:**
- ✅ Все устройства получат уведомление HIGH priority
- ✅ Обновление **обязательное** (нельзя отложить)
- ✅ Устройства обновятся в течение **1-2 часов** (не 6 часов!)
- ✅ После обновления tap/swipe заработают сразу

### Как работает автообновление?

1. **UpdateManager** проверяет обновления каждый 1 час
2. При `force_update: true` отправляется HIGH priority уведомление
3. APK скачивается в фоне (23 MB)
4. Пользователю показывается окно установки
5. После установки агент перезапускается

**Время обновления:**
- Скачивание: ~10-30 сек (зависит от интернета)
- Установка: ~10 сек
- Перезапуск: ~5 сек
- **Итого: ~30-45 секунд**

---

## 🎯 Для кого это обновление

- ✅ **500+ эмуляторов** с root доступом
- ✅ Устройства где Accessibility Service отключён
- ✅ Устройства где shell команды блокируются

---

## 🔧 Технические детали

### Изменения в коде

**CommandExecutor.kt:**
```kotlin
// До v1.8.1 (НЕ РАБОТАЛО):
suspend fun tap(x: Int, y: Int): CommandResult {
    if (SphereAccessibilityService.isServiceEnabled()) {
        return accessibilityTap(x, y)
    }
    return executeInputCommand("input tap $x $y") // ❌ Блокируется без root
}

// После v1.8.1 (РАБОТАЕТ):
suspend fun tap(x: Int, y: Int): CommandResult {
    // 1. Пытаемся через root
    if (checkRoot()) {
        return executeRootCommand("input tap $x $y") // ✅ Работает через su
    }
    
    // 2. Пытаемся через Accessibility
    if (SphereAccessibilityService.isServiceEnabled()) {
        return accessibilityTap(x, y)
    }
    
    // 3. Последняя попытка через shell
    return executeInputCommand("input tap $x $y")
}
```

### Новый метод executeRootCommand()
```kotlin
private fun executeRootCommand(command: String): CommandResult {
    val process = Runtime.getRuntime().exec("su")
    val os = DataOutputStream(process.outputStream)
    
    os.writeBytes("$command\n")
    os.writeBytes("exit\n")
    os.flush()
    os.close()
    
    val exitCode = process.waitFor()
    return CommandResult(success = exitCode == 0)
}
```

---

## 📊 Статистика обновления

**Ожидаемые результаты:**

| Время | Устройств на v1.8.0 | Устройств на v1.8.1 |
|-------|---------------------|---------------------|
| 0 мин | 500 (100%)          | 0 (0%)              |
| 30 мин| ~400 (80%)          | ~100 (20%)          |
| 1 час | ~200 (40%)          | ~300 (60%)          |
| 2 часа| ~50 (10%)           | ~450 (90%)          |
| 3 часа| 0 (0%)              | 500 (100%)          |

**Проверить статус можно через API:**
```bash
curl https://adb.leetpc.com/api/v1/agent/agents | jq '[.[] | {device: .device_model, version: .agent_version}]'
```

---

## 🚀 Как загрузить APK на GitHub

### Шаг 1: Создать Release
1. Открыть https://github.com/RootOne1337/SphereAgent-APK/releases/new?tag=v1.8.1
2. Title: `v1.8.1 - CRITICAL FIX: Root Input Commands`
3. Description: Скопировать из этого файла

### Шаг 2: Загрузить APK
1. Перетащить файл `SphereAgent-v1.8.1.apk` в область "Attach binaries"
2. Или: `gh release create v1.8.1 SphereAgent-v1.8.1.apk -t "v1.8.1 - Critical Fix" -F RELEASE_NOTES_v1.8.1.md`

### Шаг 3: Publish Release
- ✅ Set as latest release
- ✅ Publish release

---

## ⚡ Немедленное обновление (если нужно быстрее)

Если нужно чтобы устройства обновились **прямо сейчас** (не ждать 1 час):

### Вариант 1: Через Web UI
```bash
# Открыть веб-интерфейс для каждого устройства:
https://adb.leetpc.com/devices/{device_id}

# Нажать кнопку "Force Update"
```

### Вариант 2: Через API
```bash
# Отправить broadcast всем устройствам:
curl -X POST https://adb.leetpc.com/api/v1/agent/broadcast \
  -H "Content-Type: application/json" \
  -d '{
    "command": "CHECK_UPDATE",
    "force": true
  }'
```

### Вариант 3: Уменьшить интервал проверки
В `UpdateManager.kt` можно временно изменить:
```kotlin
// Было (1 час):
private const val CHECK_INTERVAL_MS = 60 * 60 * 1000L

// Сделать (5 минут):
private const val CHECK_INTERVAL_MS = 5 * 60 * 1000L
```

---

## 📝 Changelog

### Added
- ✅ `executeRootCommand()` - метод для выполнения команд через su
- ✅ Проверка root доступа в `checkRoot()`
- ✅ Логирование диагностики для всех input команд

### Changed
- ✅ Приоритет выполнения: Root → Accessibility → Shell
- ✅ `tap()` - теперь сначала пытается root
- ✅ `swipe()` - теперь сначала пытается root
- ✅ `longPress()` - теперь сначала пытается root
- ✅ `keyEvent()` - теперь сначала пытается root

### Fixed
- ✅ Input команды не работали на эмуляторах
- ✅ Root доступ был доступен, но не использовался
- ✅ Команды блокировались Android без Accessibility

---

## 🔗 Links

- **GitHub Release:** https://github.com/RootOne1337/SphereAgent-APK/releases/tag/v1.8.1
- **APK Download:** https://github.com/RootOne1337/SphereAgent-APK/releases/download/v1.8.1/SphereAgent-v1.8.1.apk
- **Changelog:** https://raw.githubusercontent.com/RootOne1337/sphere-config/main/changelog.json

---

## ❓ FAQ

**Q: Почему нужно обязательное обновление?**  
A: Без этого исправления tap/swipe/keyevent не работают на эмуляторах. Это критично для управления 500+ устройствами.

**Q: Сколько времени займёт обновление?**  
A: ~30-45 секунд на устройство. Все 500 устройств обновятся за 1-3 часа.

**Q: Можно ли отключить force_update?**  
A: Да, после того как все устройства обновятся, можно поставить `"force_update": false` в `update_info.json`.

**Q: Как проверить что обновление прошло?**  
A: Откройте устройство в веб-интерфейсе и проверьте версию. Или посмотрите логи: `adb logcat | grep "ROOT command SUCCESS"`.

**Q: Что если устройство не обновится?**  
A: Устройство будет пытаться обновиться каждый 1 час. Если после 6 часов не обновилось - проверьте интернет-соединение.

---

**Версия:** 1.8.1  
**Дата:** 2026-01-02  
**Автор:** RootOne  
**Приоритет:** CRITICAL ⚡
