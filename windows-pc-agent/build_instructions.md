# 🏗️ Сборка в ОДИН .exe файл

## 📦 Создание портабельного SpherePC-Agent.exe

### Вариант 1: PyInstaller (рекомендуется)

```bash
# Установи PyInstaller
pip install pyinstaller

# Собери .exe
python build_single_exe.py
```

**Результат:** `dist/SpherePC-Agent.exe` (~15-25 MB)

---

### Вариант 2: Auto.py to Exe (GUI)

```bash
# Установи
pip install auto-py-to-exe

# Запусти GUI
auto-py-to-exe
```

**Настройки:**
- Script: `main.py`
- One File: `✓`
- Console: `✓` (или Window Based для скрытого)
- Additional Files: `config.yaml` → `.`
- Hidden Imports: `websockets, psutil, yaml, aiohttp`

---

### Вариант 3: Nuitka (самый быстрый)

```bash
pip install nuitka

python -m nuitka --onefile --follow-imports main.py
```

---

## 🚀 Использование .exe

### Для пользователя:

1. **Скачай** `SpherePC-Agent.exe`
2. **Запусти** двойным кликом
3. **Готово!** ПК появится на сайте

**Никаких установок, никаких токенов!**

---

## 🛡️ Отказоустойчивость (встроенная!)

### 1. **Множество серверов**

```yaml
server:
  url: "https://adb.leetpc.com"
  fallback_urls:
    - "https://sphereadb-api-v2.ru.tuna.am"
    - "https://backup1.leetpc.com"
    - "https://backup2.leetpc.com"
```

### 2. **Автоматический reconnect**

```python
# Exponential backoff
1s → 2s → 4s → 8s → 16s → 32s → 60s (max)
```

### 3. **Переключение на fallback**

```python
if основной сервер недоступен:
    попробовать fallback[0]
    попробовать fallback[1]
    попробовать fallback[2]
    вернуться к основному
```

### 4. **Heartbeat мониторинг**

```python
Каждые 30 секунд:
    отправить heartbeat
    если нет ответа 90 сек:
        переподключиться
```

---

## 🌐 Веб-интерфейс (полное управление)

### Страница: `/remote-pcs`

**Доступно:**
- ✅ Список ПК (Online/Offline)
- ✅ CPU, RAM, Disk метрики
- ✅ Список эмуляторов LDPlayer
- ✅ Управление эмуляторами (запуск, остановка, создание)
- ✅ Настройки эмуляторов (RAM, CPU, разрешение)
- ✅ Установка APK
- ✅ Shell команды
- ✅ Логи агента

**Всё в браузере!**

---

## 📊 Размер .exe

| Метод | Размер | Скорость |
|-------|--------|----------|
| PyInstaller | ~20 MB | Средняя |
| Nuitka | ~15 MB | Быстрая |
| Auto.py to Exe | ~22 MB | Средняя |

**С UPX компрессией:** ~10-12 MB

---

## 🔒 Безопасность

.exe включает:
- ✅ Python interpreter
- ✅ Все библиотеки
- ✅ Конфиг по умолчанию
- ✅ Shell whitelist/blacklist
- ✅ Автоматическая регистрация (без токенов)

**Никакой установки на ПК не требуется!**

---

## 🎯 Deployment

### Массовый деплой:

1. Соберите `SpherePC-Agent.exe`
2. Положите на сетевой диск или облако
3. Пользователи скачивают и запускают
4. Все ПК появляются в системе автоматически

**Никаких токенов, никакой настройки!**

---

## 📝 Автозапуск

### Windows Task Scheduler:

```bash
# От Администратора
schtasks /create /tn "SpherePC Agent" /tr "C:\SpherePC-Agent.exe" /sc onlogon /rl highest
```

### Или Windows Service:

```bash
# Если хочешь Service (требует Python)
python install_service.py install
```

**Но с .exe проще - просто добавь в автозагрузку!**

---

## ✅ Итого

**Один .exe файл:**
- ✅ Запускается везде (Windows 10/11)
- ✅ Не требует Python
- ✅ Автоматическая регистрация
- ✅ Отказоустойчивое подключение
- ✅ Полное управление через веб

**Просто запусти - и работает!** 🚀
