# 🔥 Как получить Windows .exe

## ❌ Проблема
Текущий файл `dist/SpherePC-Agent` собран на Linux - это **не работает** на Windows!

## ✅ Решение: Собрать на Windows

### Вариант 1: Автоматическая сборка (РЕКОМЕНДУЕТСЯ)

**На Windows ПК:**

```cmd
# 1. Скачай код
git clone https://github.com/RootOne1337/SphereAgent-APK.git
cd SphereAgent-APK
git checkout feature/windows-pc-agent
cd windows-pc-agent

# 2. Запусти автосборку
BUILD_FINAL.bat

# 3. Результат:
# dist\SpherePC-Agent.exe (~20 MB)
```

**BUILD_FINAL.bat делает:**
- Проверяет Python
- Устанавливает PyInstaller
- Собирает .exe с встроенным Python и всеми библиотеками
- Один файл, готов к использованию!

---

### Вариант 2: Ручная сборка

```cmd
# Установи Python 3.10+ (python.org)

# Установи зависимости
pip install pyinstaller websockets psutil pyyaml aiohttp

# Собери
python build_single_exe.py

# Результат:
# dist\SpherePC-Agent.exe
```

---

### Вариант 3: Без сборки - Portable

Если не хочешь собирать .exe:

```cmd
# 1. Установи Python 3.10+
# 2. Установи зависимости:
pip install -r requirements.txt

# 3. Запусти:
python main.py

# Или используй portable запуск:
SpherePC-Agent-PORTABLE.bat
```

---

## 📦 Готовый .exe

**После сборки на Windows:**
- Размер: ~20 MB (было 15 MB на Linux)
- Работает: ✅ Windows 10/11
- Зависимости: НЕТ!
- Токены: НЕ НУЖНЫ!

**Использование:**
```cmd
# Просто запусти
SpherePC-Agent.exe

# Откроется консоль с логами
# ПК появится на https://adb.leetpc.com/remote-pcs
```

---

## 🎯 Почему не работает Linux сборка?

PyInstaller создаёт executable для **текущей ОС**:
- Linux → ELF binary (не работает на Windows)
- Windows → .exe (не работает на Linux)
- macOS → Mach-O (не работает на Windows/Linux)

**Решение:** Собирай на той ОС, где будешь запускать!

---

## 🚀 Быстрый способ (если есть Windows)

1. Открой PowerShell на Windows
2. Выполни:

```powershell
# Клонируй
git clone https://github.com/RootOne1337/SphereAgent-APK.git
cd SphereAgent-APK\windows-pc-agent

# Собери
.\BUILD_FINAL.bat

# Готово!
# dist\SpherePC-Agent.exe
```

3. Запусти `dist\SpherePC-Agent.exe`
4. Всё работает!

---

## ❓ Нет Windows для сборки?

**Варианты:**

1. **GitHub Actions** (автосборка):
   - Создай workflow
   - Собирает на Windows runner
   - Публикует .exe в Release

2. **Docker + Wine** (эмуляция):
   ```bash
   # Установи Wine
   sudo apt install wine python3-pip
   
   # Собери через Wine (НЕ РЕКОМЕНДУЕТСЯ - нестабильно)
   wine python build_single_exe.py
   ```

3. **Виртуалка** (Windows VM):
   - VirtualBox + Windows 10
   - Собери там

4. **Portable версия** (без .exe):
   - Просто копируй исходники
   - Запускай через `python main.py`

---

## 📝 Итого

**Лучший способ:**
1. Найди Windows ПК (хоть на 5 минут)
2. Запусти `BUILD_FINAL.bat`
3. Получи готовый .exe
4. Копируй на все ПК

**Альтернатива:**
- Используй portable версию (Python + исходники)
- Или попроси кого-то собрать .exe

---

## 🎯 Финальный .exe загрузим сюда!

После сборки на Windows:
- Создай Release на GitHub
- Attach `SpherePC-Agent.exe`
- Все смогут скачать!

