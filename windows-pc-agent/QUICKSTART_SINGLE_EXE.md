# 🚀 БЫСТРЫЙ СТАРТ - Один .exe файл

## ДЛЯ АДМИНА

### 1. Собери .exe (один раз)

```bash
cd SpherePC-Agent
pip install pyinstaller
python build_single_exe.py
```

**Результат:** `dist/SpherePC-Agent.exe` (~20 MB)

---

## ДЛЯ ПОЛЬЗОВАТЕЛЯ

### 1. Скачай

Скачай `SpherePC-Agent.exe` (один файл!)

### 2. Запусти

Двойной клик по `SpherePC-Agent.exe`

### 3. Готово!

Открой https://adb.leetpc.com/remote-pcs - увидишь свой ПК!

---

## 🛡️ ОТКАЗОУСТОЙЧИВОСТЬ

### Встроено автоматически:

✅ **4 сервера** для подключения  
✅ **Auto-reconnect** каждые 1-60 секунд  
✅ **Fallback переключение** при недоступности  
✅ **Heartbeat** каждые 30 секунд  
✅ **Автоматическая регистрация** без токенов  

### Если основной сервер упал:

```
1. Агент автоматически попробует:
   - sphereadb-api-v2.ru.tuna.am
   - backup1.leetpc.com
   - backup2.leetpc.com

2. Exponential backoff:
   1s → 2s → 4s → 8s → 16s → 32s → 60s

3. Когда основной сервер восстановится:
   - Агент автоматически переподключится
```

**ВСЁ РАБОТАЕТ САМО!**

---

## 🌐 ВЕБ-ИНТЕРФЕЙС

### Открой: https://adb.leetpc.com/remote-pcs

### Доступно:

- ✅ **Список ПК** (Online/Offline статус)
- ✅ **Метрики** (CPU, RAM, Disk в реальном времени)
- ✅ **LDPlayer управление:**
  - Список эмуляторов
  - Запуск / Остановка
  - Создание / Клонирование / Удаление
  - Настройки (RAM, CPU, разрешение, DPI, FPS)
  - Установка APK
  - Запуск приложений
- ✅ **Shell команды** (безопасные)
- ✅ **Логи** агента
- ✅ **Статистика** подключения

**ВСЁ УПРАВЛЕНИЕ В БРАУЗЕРЕ!**

---

## 🔧 НАСТРОЙКА (опционально)

### Если нужно изменить сервер:

Создай `config.yaml` рядом с .exe:

```yaml
server:
  url: "https://твой-сервер.com"
  fallback_urls:
    - "https://backup.com"

pc:
  name: "Мой ПК"
  location: "Офис"
```

**Но по умолчанию всё уже настроено!**

---

## 📦 МАССОВЫЙ ДЕПЛОЙ

### Вариант 1: Сетевой диск

```bash
# Положи .exe на сетевой диск
\\server\share\SpherePC-Agent.exe

# Отправь пользователям:
"Запусти \\server\share\SpherePC-Agent.exe"
```

### Вариант 2: Group Policy

```bash
# GPO Startup Script
Computer Configuration → Policies → Windows Settings → Scripts → Startup
Add: SpherePC-Agent.exe
```

### Вариант 3: Email

Отправь .exe по email с инструкцией: "Запусти файл"

---

## 🎯 АВТОЗАПУСК

### Windows Task Scheduler:

```batch
schtasks /create /tn "SpherePC Agent" ^
  /tr "C:\Tools\SpherePC-Agent.exe" ^
  /sc onlogon /rl highest
```

### Или в автозагрузку:

```batch
copy SpherePC-Agent.exe "%APPDATA%\Microsoft\Windows\Start Menu\Programs\Startup\"
```

---

## ❓ FAQ

### Q: Нужен ли Python?
**A: НЕТ!** .exe содержит всё необходимое.

### Q: Нужны ли токены?
**A: НЕТ!** Автоматическая регистрация по hardware_id.

### Q: Что делать если сервер недоступен?
**A: НИЧЕГО!** Агент автоматически переключится на fallback.

### Q: Как обновить агент?
**A: Просто замени .exe на новый.**

### Q: Безопасно ли это?
**A: ДА!** Shell команды фильтруются, опасные команды блокируются.

### Q: Работает ли без интернета?
**A: НЕТ,** нужен интернет для подключения к серверу.

---

## 📞 ПОДДЕРЖКА

Если ПК не появляется на сайте:

1. Проверь логи: `logs/agent.log` (рядом с .exe)
2. Проверь доступность сервера: https://adb.leetpc.com
3. Проверь firewall (разреши исходящие подключения к 443 порту)

---

## ✅ ИТОГО

**Один .exe файл = всё в одном:**

- ✅ Python interpreter
- ✅ Все библиотеки
- ✅ Конфигурация по умолчанию
- ✅ Отказоустойчивость
- ✅ Автоматическая регистрация
- ✅ LDPlayer управление
- ✅ Heartbeat мониторинг

**ПРОСТО ЗАПУСТИ - И РАБОТАЕТ!** 🎉
